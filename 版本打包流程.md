# Clipboard Image Saver - 剪贴板图片保存工具

> 轻量级 Windows 图片粘贴工具，体积仅 9KB，开源免费

[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](https://opensource.org/licenses/MIT)
[![Platform](https://img.shields.io/badge/platform-Windows-lightgrey.svg)](https://www.microsoft.com/windows)

## 简介

一个轻量级的 Windows 工具，可以通过自定义快捷键快速将剪贴板中的图片保存到当前文件夹。

**特点：**
- 🎯 自动识别当前打开的文件夹
- ⌨️ 自定义快捷键
- 🌐 中英文界面切换
- 🎨 自定义托盘图标
- 💾 超小体积 (~9KB)
- 🚀 无需安装，即开即用

## 使用方法

1. **启动程序**：双击 `ClipboardImageSaver.exe`
2. **截图或复制图片**
3. **打开目标文件夹**
4. **按快捷键**（默认 Ctrl+S）保存图片

图片会自动保存到当前文件夹，文件名格式：`image_yyyyMMdd_HHmmss.png`

### 模式：手动 / 自动

- 手动模式：在目标文件夹窗口按快捷键（默认 `Ctrl+S`）保存剪贴板图片到该文件夹
- 自动模式：开启后，监控剪贴板图片，自动保存到你指定的目录（托盘菜单可设置保存路径）

## 功能

### 自定义快捷键

右键托盘图标 → "更改快捷键" → 按下想要的组合键

### 语言切换

右键托盘图标 → "切换到英文/中文"

## 开源协议

[MIT License](LICENSE)

## 作者

网站: [sevencn.com](https://sevencn.com)

## 贡献

欢迎提交 Issue 和 Pull Request！


**⭐ 如果觉得有用，请给个 Star！**

## 发版与打包说明

### 分支提交（自动构建，不发布）
- 推送到 `main`/`master` 分支时，会自动构建并上传构建产物到 Actions 的 Artifacts
- 版本号格式：`dev-<branch>-<shortsha>`（例如 `dev-main-a1b2c3d`），用于区分每次提交
- 下载位置：GitHub → Actions → 选中对应运行 → Artifacts 区域

### 标签发布（自动打包并创建 Release）
- 创建标签并推送：
  - `git tag -a v1.2.3 -m "Release v1.2.3"`
  - `git push origin v1.2.3`
- 触发后将：
  - 注入版本号到程序 About 文本（标签名，如 `v1.2.3`）
  - 生成 `ClipboardImageSaver-v1.2.3.zip` 和 `ClipboardImageSaver.exe`
  - 在 Release 页面发布上述产物

### 手动触发构建（不发布，可自定义版本）
- GitHub → Actions → `Build and Release` → `Run workflow`
- 选填 `version`（如 `v1.2.3`），不填则使用分支名/标签名
- 仅构建与打包，不会创建 Release（除非以标签触发）

### 注意事项
- 标签需以 `v` 开头（当前匹配规则为 `v*`）
- 标签必须推送到远端：本地打标签后执行 `git push origin <tag>`
- 工作流文件路径：`.github/workflows/release.yml`